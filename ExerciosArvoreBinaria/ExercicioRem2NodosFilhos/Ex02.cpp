/*2. No código, existe a condição if (paiSucessor == filho). Explique, com um
exemplo de árvore, em qual cenário o sucessor é o filho direto à direita do
nó a ser removido.*/

//R: Se o sucessor foi filho direto na direita, um exemplo é o próprio exercicio 1 que o 80 é sucessor direto a direita do 70 que foi removido.
