/*
7. Como você alteraria as linhas do sucessor e do while para que o código
utilizasse o antecessor (o maior valor da subárvore esquerda) em vez do
sucessor?
dentro do loop while, no lugar de usar (sucessor->esq != NULL), utilizaria (sucessor->dir != NULL), pois ele buscaria
o maior valor a direita da árvore esqueda.
*/
